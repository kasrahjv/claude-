# Quick Open — Chrome Extension

Type a website (like `github.com`) or a search term, hit **Open**, and
it launches in a new tab. Save sites you use often as **Favorites** for
one-click access next time.

## Install it (same steps as before)

1. Unzip this folder.
2. Go to `chrome://extensions`.
3. Turn on **Developer mode** (top-right toggle).
4. Click **Load unpacked**.
5. Select the `quickopen-extension` folder (the one containing
   `manifest.json` — not the `icons` folder inside it).
6. Pin it to your toolbar via the puzzle-piece icon.

## How to use it

- Type a website address (`bbc.com`, `youtube.com`) → click **Open** → opens that site.
- Type anything else (`weather in Hamburg`) → click **Open** → runs a Google search for it.
- Click **★ Save** to add whatever's in the box to your Favorites list.
- Click a saved favorite any time to open it instantly.
- Click the **✕** next to a favorite to remove it.

Favorites are stored locally in your browser only — nothing is sent
anywhere else.
